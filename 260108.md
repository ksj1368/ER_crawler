# 260108.md

## Executive Summary
금일 작업은 게임 매치 데이터 내 **가젯(Gadget) 사용 정보**와 **보리(Bori) 보상 정보**를 수집 및 적재하기 위한 데이터 파이프라인 확장을 목표로 하였습니다. 이를 위해 `MatchUserGadget` 테이블을 신규 생성하고, 기존 `match_user_object` 테이블에 보리 보상 데이터를 매핑하는 파싱 로직을 구현하였으며, 관련 스키마 변경 사항을 Alembic 마이그레이션 스크립트로 반영하였습니다.

## Key Modifications & Architecture

### 1. 데이터베이스 스키마 확장 (`scripts/models.py`)
- **`MatchUserGadget` 테이블 추가**: 유저별 가젯 사용량은 `(match_id, user_num, gadget_id)`를 복합 기본키(Composite Primary Key)로 하는 1:N 구조로 설계하였습니다. 이는 가젯 데이터가 매치 내 유저별로 다수 존재할 수 있는 특성을 반영한 것입니다.
- **보리 보상 데이터 매핑**: 별도의 테이블 생성 대신 기존의 범용 메트릭 테이블인 `match_user_object`를 활용하였습니다. `metric_type='bori'`, `metric_name=grade` 형태로 매핑하여 스키마 복잡도를 낮추면서도 데이터 유연성을 확보하였습니다.

### 2. 파싱 로직 고도화 (`scripts/match_info_parsing.py`)
- **가젯 파싱 함수 구현 (`_parse_user_gadget_from_game`)**: API 응답의 `useGadget` 필드를 순회하며 DB 스키마에 맞는 리스트 형태(List[Dict])로 변환하는 전용 함수를 추가하였습니다.
- **보리 보상 파싱 함수 구현 (`_parse_user_bori_from_game`)**: `getBoriReward` 필드를 파싱하여 `match_user_object` 테이블 포맷에 맞게 변환 후, 메인 파싱 루프에서 통합되도록 처리하였습니다.

### 3. 마이그레이션 관리 (Alembic)
- **Revision `c65be9497d67` 생성**: 스키마 변경 사항을 DB에 안전하게 적용하기 위한 마이그레이션 스크립트를 작성하였습니다.

## Error Handling & Debugging

### Issue: Alembic Autogenerate 감지 실패
- **현상**: `alembic revision --autogenerate` 명령 수행 시, 모델 변경 사항(`MatchUserGadget` 추가)을 감지하지 못하고 빈 `upgrade()` 함수가 생성되는 현상이 발생하였습니다.
- **원인 (Root Cause)**: `alembic/env.py` 실행 시점과 `Base.metadata`의 로딩 시점 간의 미세한 불일치 혹은 현재 DB 상태와의 동기화 문제로 추정됩니다.
- **해결 (Solution)**: `check_metadata_tmp.py` 스크립트를 통해 `MatchUserGadget`이 `Base.metadata`에 정상적으로 등록되어 있음을 확인한 후, 마이그레이션 스크립트(`c65be9497d67_add_match_user_gadget.py`)에 테이블 생성 구문(`op.create_table`)을 수동으로 작성하여 조치하였습니다.

## Rationale

### 1. 테이블 분리 vs 통합 (Gadget vs Bori)
- **Gadget**: `gadget_id`라는 명확한 외부 식별자가 존재하고, 향후 가젯별 메타데이터와의 조인이 빈번할 것으로 예상되어 정규화된 별도 테이블로 분리하였습니다.
- **Bori Reward**: 단순히 상자 등급(Grade)과 횟수(Count)로 구성된 데이터이므로, 이를 위해 별도 테이블을 생성하는 것은 오버헤드라고 판단하였습니다. 따라서 기존의 Key-Value 구조인 `match_user_object`에 통합하여 관리 효율성을 높였습니다.

### 2. 파싱 로직의 모듈화
- 파싱 로직을 거대한 하나의 함수에 넣지 않고 `_parse_user_gadget_from_game`과 같이 단위 함수로 분리하였습니다. 이는 코드 가독성을 높이고, 추후 API 구조 변경 시 해당 함수만 수정하면 되므로 유지보수성을 향상시킵니다.

### 3. 오브젝트 데미지 컬럼 통합
- `match_user_object` 테이블과 `match_user_damage`에 중복 저장하던 오브젝트(루미, 야생동물) 데미지 데이터를 `match_user_damage` 테이블만 저장하도록 수정하여 각 테이블의 직관성을 명확히하고 단순화하였습니다.

---

## Impact & Test Results

- **데이터 수집 범위 확대**: 기존에 누락되었던 가젯 활용 전략 및 보리 보상 획득 패턴 분석이 가능해졌습니다.
- **메타데이터 검증**: 별도 테스트 스크립트(`check_metadata_tmp.py`) 수행 결과, 신규 모델이 ORM 메타데이터에 정상적으로 로드됨을 확인하였습니다.
- **안정적 배포 준비**: 수동 검증된 마이그레이션 스크립트를 통해 운영 DB에 즉시 배포 가능한 상태를 확보하였습니다.
